# Dependencias
node_modules/

# Build
dist/
dist-ssr/
*.local

# Variables de entorno — .env.example SÍ se sube (plantilla sin datos reales)
.env
.env.*
!.env.example

# Cloudflare Workers (wrangler)
.wrangler/
.dev.vars
.dev.vars.*

# Supabase CLI — estado y caché local del enlace al proyecto
supabase/.temp/
supabase/.branches/

# Capacitor / nativo: los proyectos android/ e ios/ SÍ se suben (llevan
# permisos y ajustes editados a mano), pero cada uno trae su propio
# .gitignore para el ruido de compilación (Gradle, Pods, DerivedData, etc.)

# Editores / SO
.DS_Store
.vscode/
.idea/
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
